node_modules
dist
public/images/vegetables-source.png
.DS_Store

# Laravel backend
backend/vendor
backend/.env
backend/.env.*
!backend/.env.example
backend/storage/*.key
spaf-hostinger.zip

# deploy credentials (the script itself is safe to commit)
deploy.conf

# local env
.env.local
.env.*.local
